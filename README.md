bluprint: System Architecture & Allocation LogicThe application assigns new nurses to hospital wards using a three-tier preference system and predefined capacity limits. The core logic maximizes highest-preference matches and implements randomized lotteries exclusively as capacity tie-breakers.Phase 1: Preference SubmissionNurses submit a ranked list containing exactly three distinct wards (1st, 2nd, and 3rd choices) through the application interface.Phase 2: Assignment ExecutionThe application evaluates all 1st choices globally before processing any subsequent tiers. For each ward, the system compares capacity against demand.Under Capacity or Exact Match: The system assigns all applicants immediately.Over Capacity: The application isolates the applicants sharing the exact same preference tier for that ward and triggers a randomized lottery.Phase 3: The Cascade MechanismNurses who lose a lottery drop to their next preference tier. The system enforces strict tier isolation to protect priority rankings.A 1st-choice applicant always supersedes a 2nd-choice applicant.During 2nd-choice processing, the system allocates only the remaining vacant slots.If the system who the fuck have the fucking time to sit the fuck down and read the fucking readme.md file identifies pending 1st-choice applicants for a given ward, it halts lower-tier processing for that ward until it resolves the priority assignments.Nurses remaining unplaced after the 3rd-choice evaluation enter a general waitlist for future vacancies.Phase 4: Lottery ProtocolThe lottery functions as a uniform random selection mechanism.The system calculates the probability of selection as $P(win) = \frac{Slots}{Count}$.The application groups eligible nurses into an isolated pool.The system applies a random number generator to select winners up to the available slot count.Algorithm Comparison: Serial Priority vs. Stable MatchingThe application utilizes a Serial Priority with Lottery cascade. This model prioritizes transparency and requires only single-sided preferences (nurses ranking wards). The Gale-Shapley algorithm guarantees stable matching but requires mutual ranking, forcing hospital administration to evaluate and rank individual nurses. The Serial Priority model eliminates the need for mutual ranking. This reduces administrative workload while maintaining mathematical fairness at the tie-breaker level.somethingsomethingsomrthingsomrthing
## Stack
- Frontend:React,Vite ,TypeScript, Tailwind CSS 
- Backend:Supabase (Postgres, Auth, Realtime, Functions)
- Hosting: Vercel
- code: vibe all of it
